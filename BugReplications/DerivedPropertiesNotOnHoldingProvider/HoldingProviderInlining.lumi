@keysToCatalog = values
('Holding/DERV_PROP_TEST/HoldingFromPortfolioDerivedProperty', 'HoldingFromPortfolioDerivedProperty', False, 'HoldingFromPortfolioDerivedProperty');

@config = select column1 as [Key], column2 as Name, column3 as IsMain, column4 as Description from @keysToCatalog;

select * from Sys.Admin.Lusid.Provider.Configure
where Provider = 'Lusid.Portfolio.Holding'
and Configuration = @config;